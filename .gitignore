__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
raymarching_benchmark.egg-info/

# runtime outputs
results/
*.png
*.npy

# generated analysis artifacts (regenerable from the sweep engine)
# ground-truth + method captures (.npz / meta.json)
references/
# sweep datasets (machine-specific provenance)
sweep.jsonl
sweep_*.jsonl
# discovery feature cache + reports (regenerable: report.features / report.discovery)
features.jsonl
discovery_*.md
# scheduled-run logs
*.log
# generated comparison / analysis reports
*.html
.pytest_cache/
